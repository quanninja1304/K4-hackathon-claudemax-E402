GEMINI_API_KEY="Điền_API_Key_của_bạn_vào_đây"

# Kết nối Qdrant Cloud (bỏ trống 2 dòng này nếu muốn dùng Qdrant local trên disk)
QDRANT_URL=""
QDRANT_API_KEY=""

# (Tuỳ chọn) Ghi đè model Gemini. Mặc định code dùng: gemini-2.5-flash
# Chỉ thêm dòng dưới nếu tài khoản của bạn KHÔNG truy cập được gemini-2.5-flash
# (thường gặp lỗi 404 "no longer available to new users"):
# GEMINI_MODEL=gemini-flash-latest
